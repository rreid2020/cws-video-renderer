name: Queue - Pick Next Topic

on:
  workflow_dispatch:

jobs:
  queue_process:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install FFmpeg (robust)
        run: |
          set -euxo pipefail
          export DEBIAN_FRONTEND=noninteractive
          sudo apt-get update -y
          sudo apt-get install -y ffmpeg
          ffmpeg -version

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install google-api-python-client google-auth google-auth-httplib2

      - name: Create output folder
        run: mkdir -p out

      - name: Pick next NEW topic -> PROCESSING
        env:
          GOOGLE_SHEETS_SA_JSON: ${{ secrets.GOOGLE_SHEETS_SA_JSON }}
          SHEET_ID: ${{ secrets.SHEET_ID }}
          SHEET_TAB: ${{ secrets.SHEET_TAB }}
        run: |
          python src/sheets_queue.py > out/picked.log

      - name: Parse picked row
        id: parse
        run: |
          python - <<EOF
import json, re, pathlib, sys

log = pathlib.Path("out/picked.log").read_text(encoding="utf-8")
m = re.search(r'(\{[\s\S]*\})', log)

if not m:
    print("No NEW topic found.")
    sys.exit(0)

picked = json.loads(m.group(1))
pathlib.Path("out/row.json").write_text(json.dumps(picked), encoding="utf-8")
print("Picked row:", picked["sheet_row"])
EOF

      - name: Generate script + metadata
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          python - <<EOF
import json, subprocess

picked = json.load(open("out/row.json"))
topic = picked["topic"]

subprocess.check_call([
    "python", "src/openai_generate.py",
    "--topic", topic,
    "--out", "out/generated.json"
])
EOF

      - name: Write script to sheet
        env:
          GOOGLE_SHEETS_SA_JSON: ${{ secrets.GOOGLE_SHEETS_SA_JSON }}
          SHEET_ID: ${{ secrets.SHEET_ID }}
          SHEET_TAB: ${{ secrets.SHEET_TAB }}
        run: |
          python - <<EOF
import json, subprocess

picked = json.load(open("out/row.json"))
row = picked["sheet_row"]

subprocess.check_call([
    "python", "src/sheets_update_processing.py",
    "--row", str(row),
    "--json", "out/generated.json"
])
EOF

      - name: Generate TTS voice.mp3
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          python src/tts_openai.py --json out/generated.json --out out/voice.mp3

      - name: Render vertical short (FFmpeg)
        run: |
          python src/render_short.py --json out/generated.json --audio out/voice.mp3 --out out/video.mp4

      - name: Upload rendered video artifact
        uses: actions/upload-artifact@v4
        with:
          name: rendered-video
          path: out/video.mp4
